name: deploy-preview

on:
  workflow_dispatch:
  pull_request:

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Deploy sample worker preview
        run: |
          echo "Replace this with wrangler deploy logic for preview environments"
          echo "Report deployment metadata back to the control plane"
