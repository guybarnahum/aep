# GitHub workflow templates

This directory is reserved for reusable workflow templates that may later be copied or generated into managed repos.

Current repo state:

- the authoritative live workflow architecture is under `.github/workflows/`
- see `.github/workflows/README.md` for the current deploy, validation, preview destroy, and preview reaper design
- this directory is not the active workflow implementation used by the repo itself today
